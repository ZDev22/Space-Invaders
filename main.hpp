#ifndef MAIN_HPP
#define MAIN_HPP

extern short wave;
extern bool bossAlive;

#include <string>

int generateRandomNumber(short min, short max);

extern short playerHealth;
extern std::string playerShip;

#endif